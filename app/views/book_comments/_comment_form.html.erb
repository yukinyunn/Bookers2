<%= form_with(model:[book, comment], id: "new_comment") do |f| %>
  <%= f.text_area :comment, rows: '5', class: "form-control" %>
  <div class="actions row">
    <%= f.submit "送信", class: "form_submit, btn btn-light" %>
  </div>
<% end %>